import { Route } from '@angular/router';
import { AddressCepComponent } from './address-cep/address-cep.component';

export const addressCepRoutes: Route[] = [
  { path: '', component: AddressCepComponent },
];
