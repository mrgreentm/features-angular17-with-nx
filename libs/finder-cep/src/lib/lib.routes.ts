import { Route } from '@angular/router';
import { FinderCepComponent } from './finder-cep/finder-cep.component';

export const finderCepRoutes: Route[] = [
  { path: '', component: FinderCepComponent },
];
